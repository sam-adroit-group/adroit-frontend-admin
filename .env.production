# Production Environment Variables
# These values should be obtained from Terraform outputs after deployment
# Run: cd ../adroit-terraform && terraform output

# Backend API URL (from API Gateway)
# Get from: terraform output api_gateway_url
VITE_API_URL=

# AWS Region
VITE_AWS_REGION=us-east-2

# Cognito User Pool ID
# Get from: terraform output cognito_user_pool_id
VITE_USER_POOL_ID=us-east-2_fJa6IKCn5

# Cognito Client ID
# Get from: terraform output cognito_client_id
VITE_USER_POOL_WEB_CLIENT_ID=j0g9bpikt4dq50girdmcfd40g

# Cognito OAuth Domain
# Get from: terraform output cognito_domain
VITE_OAUTH_DOMAIN=adroit-admin-df86bf16.auth.us-east-2.amazoncognito.com

# OAuth Redirect URLs
# IMPORTANT: Use your GoDaddy custom domain (not the Amplify URL)
# This must match the domain where users access your application
# Example: https://admin.yourdomain.com/ or https://yourdomain.com/
# Also ensure these URLs are configured in Cognito User Pool as allowed callback URLs
VITE_REDIRECT_SIGN_IN=https://admin.adroverse.co
VITE_REDIRECT_SIGN_OUT=https://admin.adroverse.co
